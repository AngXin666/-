# 打包脚本最终审查报告

## 审查时间
2026-02-17

## 审查范围
- ✅ build_exe.py 打包脚本
- ✅ runtime hooks 文件
- ✅ PyInstaller hooks 文件
- ✅ 源代码中的subprocess调用
- ✅ 源代码中的os.system调用
- ✅ .spec文件配置

---

## 发现的问题

### ⚠️ 问题1: crypto_utils.py 使用 shell=True

**位置**: `src/crypto_utils.py` 第384行

**代码**:
```python
result = subprocess.check_output(
    'wmic csproduct get uuid', 
    shell=True  # ⚠️ 这会弹出CMD窗口
)
```

**影响**: 
- 在打包后的程序中，这个调用会弹出CMD窗口
- 即使有runtime hook，`shell=True`的调用仍然可能弹出窗口

**严重程度**: 🔴 高（会导致CMD窗口弹出）

**修复方案**:
```python
# 方案1: 不使用shell=True，直接调用wmic
result = subprocess.check_output(
    ['wmic', 'csproduct', 'get', 'uuid'],
    # shell=True  # 删除这行
)

# 方案2: 如果必须使用shell=True，添加额外的隐藏参数
if platform.system() == 'Windows':
    startupinfo = subprocess.STARTUPINFO()
    startupinfo.dwFlags |= subprocess.STARTF_USESHOWWINDOW
    startupinfo.wShowWindow = subprocess.SW_HIDE
    
    result = subprocess.check_output(
        'wmic csproduct get uuid',
        shell=True,
        startupinfo=startupinfo,
        creationflags=subprocess.CREATE_NO_WINDOW
    )
```

**推荐**: 使用方案1（不使用shell=True）

---

### ⚠️ 问题2: run.py 使用 os.system

**位置**: `run.py` 第26行

**代码**:
```python
try:
    # 静默执行，不显示输出
    os.system('chcp 65001 >nul 2>&1')  # ⚠️ 这会弹出CMD窗口
except:
    pass
```

**影响**:
- `os.system`会创建新的CMD窗口
- 即使有runtime hook，`os.system`不受subprocess patch影响

**严重程度**: 🟡 中（程序启动时会短暂弹出CMD窗口）

**修复方案**:
```python
# 方案1: 使用subprocess替代os.system
try:
    if sys.platform == 'win32':
        subprocess.run(
            ['chcp', '65001'],
            capture_output=True,
            check=False
        )
except:
    pass

# 方案2: 在打包环境中跳过这个调用
try:
    if sys.platform == 'win32' and not getattr(sys, 'frozen', False):
        # 只在开发环境中执行
        os.system('chcp 65001 >nul 2>&1')
except:
    pass

# 方案3: 完全移除（打包后不需要chcp）
# 因为打包后的程序已经在代码中设置了UTF-8编码
# 这个os.system调用可能是多余的
```

**推荐**: 使用方案3（移除这行代码）

**理由**: 
- 打包后的程序已经通过Python代码设置了UTF-8编码
- `sys.stdout = io.TextIOWrapper(..., encoding='utf-8')`已经处理了编码问题
- 不需要再调用chcp命令

---

### ✅ 问题3: .spec文件中的runtime_hooks路径

**位置**: `溪盟商城自动化助手.spec` 第17行

**当前配置**:
```python
runtime_hooks=['pyi_rth_hide_console.py', 'pyi_rth_subprocess.py', 'pyi_rth_asyncio_fix.py'],
```

**状态**: ⚠️ 使用相对路径

**说明**:
- 当前.spec文件使用相对路径
- 但build_exe.py已经修复为使用绝对路径
- 下次打包时会生成新的.spec文件，会使用绝对路径

**建议**: 
- 删除旧的.spec文件，让PyInstaller重新生成
- 或者手动修改.spec文件使用绝对路径

---

## 其他检查项

### ✅ 已正确配置的部分

1. **Runtime hooks执行顺序** ✅
   - pyi_rth_hide_console.py (第一)
   - pyi_rth_subprocess.py (第二)
   - pyi_rth_asyncio_fix.py (第三)

2. **subprocess调用** ✅
   - `src/emulator_controller.py`: 所有调用都会被runtime hook patch
   - `src/adb_bridge.py`: 所有调用都会被runtime hook patch
   - ⚠️ `src/crypto_utils.py`: 有一个使用shell=True的调用（需要修复）

3. **模块导入** ✅
   - 所有src模块都已包含在hidden_imports中
   - asyncio模块导入顺序正确
   - multiprocessing支持已添加

4. **数据文件** ✅
   - config目录
   - models目录
   - 配置文件（config.yaml等）

5. **打包脚本功能** ✅
   - 使用绝对路径指定runtime hooks
   - 添加了验证步骤
   - 添加了打包结果检查

---

## 修复优先级

### 🔴 高优先级（必须修复）

1. **修复crypto_utils.py中的shell=True**
   - 这会导致CMD窗口弹出
   - 影响用户体验

### 🟡 中优先级（建议修复）

2. **修复run.py中的os.system**
   - 会在启动时短暂弹出CMD窗口
   - 可以通过移除代码解决

### 🟢 低优先级（可选）

3. **删除旧的.spec文件**
   - 让PyInstaller重新生成
   - 确保使用最新配置

---

## 详细修复步骤

### 步骤1: 修复crypto_utils.py

**修改文件**: `src/crypto_utils.py`

**查找**:
```python
result = subprocess.check_output(
    'wmic csproduct get uuid', 
    shell=True
)
```

**替换为**:
```python
result = subprocess.check_output(
    ['wmic', 'csproduct', 'get', 'uuid']
)
```

### 步骤2: 修复run.py

**修改文件**: `run.py`

**查找**:
```python
try:
    # 静默执行，不显示输出
    os.system('chcp 65001 >nul 2>&1')
except:
    pass
```

**替换为**:
```python
# 打包后不需要chcp命令，因为已经通过Python代码设置了UTF-8编码
# 如果需要在开发环境中使用，可以添加条件判断
try:
    if sys.platform == 'win32' and not getattr(sys, 'frozen', False):
        # 只在开发环境中执行
        os.system('chcp 65001 >nul 2>&1')
except:
    pass
```

**或者直接删除**:
```python
# 删除这段代码，因为打包后不需要
```

### 步骤3: 删除旧的.spec文件

```bash
del 溪盟商城自动化助手.spec
```

### 步骤4: 重新打包

```bash
python build_exe.py
```

---

## 验证清单

打包完成后，请验证以下项目：

### 启动测试
- [ ] 双击exe文件启动程序
- [ ] 检查是否弹出CMD窗口
- [ ] 检查程序是否正常显示

### 功能测试
- [ ] 添加账户（检查是否弹出CMD窗口）
- [ ] 登录功能（检查是否弹出CMD窗口）
- [ ] 签到功能（检查是否弹出CMD窗口）
- [ ] 转账功能（检查是否弹出CMD窗口）

### 日志检查
- [ ] 查看logs目录中的日志文件
- [ ] 检查是否有错误信息
- [ ] 确认所有功能正常执行

---

## 预期效果

修复后，打包的程序应该：
- ✅ 启动时不弹出CMD窗口
- ✅ 运行时不弹出CMD窗口
- ✅ 所有subprocess调用都被正确patch
- ✅ 所有功能正常工作

---

## 风险评估

### 修复crypto_utils.py
- **风险**: 低
- **影响**: 只是改变调用方式，功能不变
- **测试**: 确认机器UUID仍然能正确获取

### 修复run.py
- **风险**: 极低
- **影响**: 移除不必要的代码
- **测试**: 确认UTF-8编码仍然正常工作

---

## 总结

### 当前状态
- ✅ 打包脚本配置正确
- ✅ Runtime hooks配置正确
- ⚠️ 有2个地方会导致CMD窗口弹出（需要修复）

### 需要修复的问题
1. 🔴 `src/crypto_utils.py` 第384行：移除`shell=True`
2. 🟡 `run.py` 第26行：移除或条件化`os.system`调用

### 修复后
- 所有CMD窗口问题都会被解决
- 程序可以正常打包和运行
- 用户体验良好

---

## 下一步操作

1. ⏳ 修复crypto_utils.py
2. ⏳ 修复run.py
3. ⏳ 删除旧的.spec文件
4. ⏳ 重新打包
5. ⏳ 测试所有功能

修复完成后，打包脚本就完全没有问题了！
